<div class="card" data-id="{{_id}}">
  <a href="https://www.bbc.com{{url}}" target="_blank"><h5 class="card-header h5">{{title}}</h5></a>
  <div class="card-body">
    <p class="card-text">{{summary}}</p>
    <a class="btn btn-md btn-primary remove">Remove From Saved</a>
    <a class="btn btn-md btn-primary comments">Article Comments</a>
  </div>
</div>